export interface BudgetStats {
  budgetApproved: number; // Orçamento aprovado
  budgetDelivered: number; // Orçamento entregue
  budgetDesapproved: number; // Orçamento reprovado
  budgetGenerated: number; // Orçamento gerado
  leads: number; // Número de leads
}
